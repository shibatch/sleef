language: c

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y libmpfr-dev
    
compiler:
    - gcc

before_script:
    - make
    - cd src
    - cd libm-tester

script: make

# safelist
branches:
  only:
  - master
  - travis